include $(TOPDIR)/rules.mk

LUCI_TITLE:=Freifunk Berlin configuration wizard
LUCI_DEPENDS:=community-profiles
LUCI_EXTRA_DEPENDS:=luci-compat, luci-mod-admin-full, falter-policyrouting, luci-lib-jsonc, luci-lib-ipkg
PKG_RELEASE:=2

include ../include-luci.mk

#  URL:=http://berlin.freifunk.net

# call BuildPackage - OpenWrt buildroot signature
